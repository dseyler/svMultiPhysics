#include "active_stress.h"

// All active-stress parameter resolution is header-inline (see
// resolve_active_stress_params in active_stress.h). This translation unit is
// intentionally minimal but kept in the build so the header has an owning .cpp.

namespace active_stress {

} // namespace active_stress
